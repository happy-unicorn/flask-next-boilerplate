import { takeLatest, put, call, delay } from 'redux-saga/effects';
import {
    productActions
} from '../reducers/product';
// import {} from '../endpoints/product';

function* a() {
    try {
    } catch (error) {

    }
}

export default [];